toggl_cred = "username:password"
toggl_api_token = "token"

jira_user = "vitali_prenger"
jira_token = "token"

eucon_gitlab_api_token = "token"

# Microsoft Graph API (Outlook 365)
ms_graph_client_id = "your-azure-app-client-id"
ms_graph_tenant_id = "your-tenant-id-or-common"

# Email notification for server authentication (optional)
notification_email = "your-email@example.com"

# Nextcloud CalDAV
nextcloud_url = "https://your-nextcloud.com"
nextcloud_username = "your-username"
nextcloud_password = "your-app-password"  # Use app password, not main password
nextcloud_calendar_name = "outlook-sync"  # Calendar to sync to
